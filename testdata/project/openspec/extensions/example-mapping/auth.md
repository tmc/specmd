# Example Mapping: Authentication

## Rules

- Users with valid credentials can log in.
- Users with invalid credentials see an error.

## Examples

- Valid credentials return a token.
- Invalid credentials do not create a session.

## Questions

- How many failed attempts should trigger lockout?
