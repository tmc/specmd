id = "openspec-lsp"
name = "OpenSpec LSP"
description = "OpenSpec Markdown diagnostics, completions, hover, symbols, definitions, and references."
version = "0.1.0"
schema_version = 1
authors = ["tmc"]
repository = "https://github.com/tmc/openspec"

[language_servers.openspec-lsp]
name = "OpenSpec LSP"
languages = ["Markdown"]

